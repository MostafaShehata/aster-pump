# Local wrapper image for the Qdrant vector database used by the RAG PoC.
# Keeping this Dockerfile in its own folder makes the vector DB component
# repo-ready and lets the root build script produce all images locally.
FROM qdrant/qdrant:v1.12.5
